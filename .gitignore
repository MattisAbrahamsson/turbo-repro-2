node_modules
yarn-error.log
**/._.DS_Store
**/.DS_Store
._.DS_Store
.DS_Store
.env
.env.*
tsconfig.tsbuildinfo
.next
.turbo


.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# This is an artifact in our dev process, so we don't want to commit it
# It's also needed so `turbo watch` doesn't watch that file
*.module.css.d.ts
